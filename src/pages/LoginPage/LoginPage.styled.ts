import { Box, styled } from "@mui/material";

export const LoginPageBox = styled(Box)(() => ({
  width: "100vw",
  height: "100vh",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
}));
