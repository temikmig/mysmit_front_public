import { LoginForm } from "@features/auth";

import { LoginPageBox } from "./LoginPage.styled";

export const LoginPage = () => {
  return (
    <LoginPageBox>
      <LoginForm />
    </LoginPageBox>
  );
};
